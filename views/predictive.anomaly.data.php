<?php
/**
 * View: predictive.anomaly.data (JSON response)
 * Data is set via CControllerResponseData in the controller.
 * Zabbix renders this as JSON automatically when response data is set.
 */
echo json_encode($data);
